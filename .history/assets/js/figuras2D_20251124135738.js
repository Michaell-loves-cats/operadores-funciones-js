funtion NumeroAlAzar(2, 10) {
    min = Math.ceil(min);
    max = Math.floor(max);
    retun Math.floo(Math.random() * (max /min + 1) + min);
}
//Codigo de developermozilla.org

let a, b, B, c, d, D, h;
const 


document.getElementById("TriAP").innerHTML = ""